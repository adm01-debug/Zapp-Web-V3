export {
  DEFAULT_EXTERNAL_URL,
  DEFAULT_EXTERNAL_KEY,
  MCP_SERVER_URL,
  useConnections,
} from '@/hooks/useConnections';
export type { SystemConnectionPayload, SystemConnection } from '@/hooks/useConnections';
