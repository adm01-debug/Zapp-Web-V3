# Tags de rollback protegidas da limpeza automática GHCR.
# Uma SHA por linha (12 primeiros caracteres do SHA do commit de produção).
# Para adicionar: bash scripts/update-rollback-protection.sh <sha12>
# Usado em: .github/workflows/deploy-vps.yml → GHCR retention (ignore-versions)
#
# Formato do ignore-versions gerado: ^production-(sha1|sha2|sha3)$
# Protege: o index OCI tagueado + seus manifests-filho (amd64 + attestation).
#
# Atualizado em 2026-08-07 (validação exaustiva VAL-03 — F1): as SHAs anteriores
# (01d83bf86c47, a123fd625d0b, ebfa029b4032) NUNCA foram head de run deploy —
# proteção fictícia (tags production-<sha> nunca existiram no GHCR; verificado
# via registry v2 tags/list). Substituídas pelas ÚNICAS tags reais existentes:
#   production-57b05d295af8 e production-7af6ecd0d51b
# (tags/list 2026-08-07: apenas estas 2 + production-latest móvel).
57b05d295af8
7af6ecd0d51b
