-- =============================================================================
-- OBSOLETA - NAO EXECUTAR
-- =============================================================================
-- Esta migration NUNCA pode ser aplicada. Ela aborta logo no inicio.
--
-- Erros confirmados contra o banco de producao em 2026-07-26:
--
--   1) CREATE INDEX ... ON zapp.contact_intelligence (phone)
--      -> ERRO: column "phone" does not exist
--      A coluna phone existe apenas na VIEW public.contact_intelligence,
--      nao na tabela base zapp.contact_intelligence.
--
--   2) CHECK (contact_id ~* '^[0-9a-f]{8}-...')
--      -> ERRO: operator does not exist: uuid ~* unknown
--      contact_id e do tipo uuid, que ja garante o formato por definicao.
--      A constraint era impossivel e desnecessaria.
--
-- Problemas adicionais que foram corrigidos na versao consolidada:
--
--   3) GRANT EXECUTE ... TO anon numa funcao SECURITY DEFINER que faz UPDATE
--      em zapp.whatsapp_connections. Qualquer chamador anonimo poderia
--      forcar o status de qualquer instancia para 'unknown'.
--
--   4) GRANT SELECT da materialized view para anon. MV nao respeita RLS, e
--      tanto role_permissions quanto permissions tem RLS ativo. Isso expunha
--      a matriz completa de permissoes para usuarios nao autenticados.
--
--   5) O trigger de updated_at em role_permissions era guardado por
--      "IF coluna updated_at EXISTS". A coluna nao existia -> no-op silencioso.
--
--   6) Os indices dos improvements 1 e 2 eram redundantes:
--      - contact_intelligence_contact_id_key (UNIQUE) ja cobria contact_id
--      - idx_messages_contact_created_active ja lidera por contact_id
--      O segundo criaria ainda um indice em tabela particionada com 23
--      particoes, sem ganho mensuravel.
--
-- SUBSTITUIDA POR:
--   20260727120000_qa_round_2_3_corrigido_consolidado.sql
--   (aplicada e validada em producao)
-- =============================================================================

SELECT 'MIGRATION OBSOLETA - ver 20260727120000_qa_round_2_3_corrigido_consolidado.sql' AS aviso;
