# FASE 3 — Reconciliação de CONFIG (etapas 35–50)

> Auditoria **read-only** do ambiente AtomicaBR — Supabase self-hosted (Docker Swarm, VPS).
> Data: 2026-08-04 · Autor: subagente Fase 3 (Hermes)
> Método: `portainer_get_stack_file` (stack `supabase` id=35, `zapp-web-prod` id=157), `portainer_exec_container` (fingerprints e verificações **dentro** dos containers — nenhum segredo sai do host), `supabase_db_query` (SQL), leitura do repo `C:\zapp-web-v3`.
> Regra de segredos: nenhum valor cru reproduzido; apenas fingerprints `sha256|cut -c1-12`. Senhas/keys do compose foram verificadas por fingerprint, nunca impressas.

---

## Resumo executivo

| Severidade | Qtd | Itens |
|---|---|---|
| **P0** | **0** | — |
| **P1** | **1** | #42 Google OAuth não configurado (login Gmail indisponível) |
| **P2** | **7** | #38 cross-tenant (nota), #41 domínios legados na allowlist, #43 imgproxy sem signing key + 8 buckets vazios, #47 VAULT_ENC_KEY placeholder, #35 segredo duplicado em texto plano no compose, #36 rotação de anon/service keys (exp 2029) |
| OK | 12 | JWT consistency, keys JWT, schemas, URLs, GoTrue, storage, Kong, realtime, analytics, pooler, timezone, cross-check |

**Achado central (35/36):** todos os componentes (auth, rest, storage, functions, realtime, kong, db, supavisor) usam **o mesmo JWT secret** — o Docker secret `supabase_jwt_secret_v1` (fingerprint do conteúdo: `b6177a99676f`), idêntico ao literal `PGRST_JWT_SECRET` do compose (fingerprint `b6177a99676f`). As keys `anon` e `service_role` foram **verificadas por assinatura HMAC-SHA256** contra esse secret (OK) e aceitas em runtime (REST 200). **Nenhum P0 de JWT.**

---

## Matriz CONFIG (etapas 35–50)

| # | Item | Esperado | Encontrado | Status | Sev | Evidência | Ação recomendada |
|---|---|---|---|---|---|---|---|
| 35 | JWT secret consistente (auth×rest×storage×functions×realtime×kong×db×supavisor) | Mesmo secret em todos | Secret único `supabase_jwt_secret_v1`; fingerprint conteúdo `b6177a99676f` == literal do compose `b6177a99676f`; auth/storage/functions/realtime/kong/supavisor leem o secret file; rest/db/studio usam o literal | **OK** | — | stack id=35 (commands `export …=$$(cat /run/secrets/supabase_jwt_secret_v1)`); exec db: `JWT_CONTENT_FP=b6177a99676f`; exec functions: `JWT_SECRET_FILE_FP=b6177a99676f`; python: fp literal `b6177a99676f` | Centralizar: remover literais do compose (rest/db/studio/realtime METRICS) e usar o secret file em todos — evita P0 futuro na rotação |
| 36 | anon/service_role keys válidas, role correta, assinadas com o mesmo secret | role=anon / role=service_role; sig válida | Payload ANON: `{"role":"anon","iss":"supabase","iat":1715050800,"exp":1872817200}`; payload SERVICE: `{"role":"service_role",…}` (iat 2024-05-07, exp 2029-05-07); `ANON_SIG=OK` (openssl in-container vs secret file); verificação local vs literal: anon True, service True; REST `/rest/v1/`=200 e storage `/storage/v1/status`=200 com a anon key | **OK** | — | exec functions: `ANON_SIG=OK`, `REST_STATUS=200`, `STORAGE_STATUS=200`, payloads decodificados; python: `ANON_KEY sig vs literal: True`, `SERVICE_ROLE_KEY sig vs literal: True` | Planejar rotação das keys antes de 2029-05 (P2); usar chave por ambiente |
| 37 | PGRST_DB_SCHEMAS (rest) cobre schemas necessários; evo/email_app NÃO expostos | public,zapp,storage,graphql_public presentes; evo/email_app fora | `PGRST_DB_SCHEMAS=public,zapp,storage,graphql_public,artes,vendas,financeiro` — os 7 existem no `pg_namespace` (28 schemas); evo e email_app **não** expostos | **OK** | — | stack id=35 (rest env); SQL `pg_namespace` (28 linhas: zapp, public, storage, graphql_public, artes, vendas, financeiro, evo, email_app…) | — |
| 38 | Superfície cross-tenant (artes/vendas/financeiro no mesmo PostgREST) | Isolamento por RLS | Mesmo PostgREST expõe zapp+artes+vendas+financeiro para anon/service_role; RLS **100% habilitado**: zapp 321/321, artes 2/2, vendas 14/14, financeiro 16/16 (policies: zapp 736, financeiro 46, vendas 28, artes 5); anon/service_role sem LOGIN | **RISK** | P2 | SQL RLS por schema; SQL `pg_roles` (anon/service_role `rolcanlogin=false`); SQL `pg_policies` | Isolamento hoje depende só de RLS/policies — documentar; avaliar chave service_role dedicada por domínio ou BFF para artes/financeiro |
| 39 | PGRST_DB_URI (rest): host/porta/role corretos | db:5432, role authenticator | `postgres://authenticator:<SECRETO>@db:5432/postgres`; login funcional `AUTHENTICATOR_OK as authenticator` com a senha do secret file; senha literal do URI == senha real (fp `b0e6e79fbc9d` == `b0e6e79fbc9d`); `PGRST_DB_ANON_ROLE=anon` | **OK** | — | stack id=35 (rest env); exec db: `AUTHENTICATOR_OK`, `DB_CONTENT_FP=b0e6e79fbc9d`; python fp literal `b0e6e79fbc9d` | — |
| 40 | Edge DB URL (functions) aponta para o MESMO backend; db-client força schema zapp | SUPABASE_URL/DB_URL → mesmo Supabase | functions: `SUPABASE_URL=http://kong:8000` (interno), `SUPABASE_DB_URL=postgresql://postgres:<SECRETO>@db:5432/postgres` (mesmo backend); command exporta `SELFHOSTED_SUPABASE_URL=https://supabase.atomicabr.com.br`; `db-client.ts` prioriza SELFHOSTED_SUPABASE_URL e força `db:{schema:"zapp"}` (linhas 26-58) | **OK** | — | stack id=35 (functions command/env); repo `supabase/functions/_shared/db-client.ts:41-58` (`createClient(…,{db:{schema:"zapp"}})`) | Nota: `PROMOGIFTS_SUPABASE_URL=https://doufsxqlfjyuvxuezpln.supabase.co` (cloud) existe no env — integração promo-gifts intencional, não é o backend principal (INFO) |
| 41 | GoTrue SITE_URL/URI_ALLOW_LIST batem com o domínio do front; sem localhost em prod | zapp.atomicabr.com.br; sem localhost | `GOTRUE_SITE_URL=https://zapp.atomicabr.com.br` == domínio prod do front (stack zapp-web-prod: Traefik `Host(\`zapp.atomicabr.com.br\`)`); `URI_ALLOW_LIST=https://whats-your-line.lovable.app,https://zapp.atomicabr.com.br,https://supabase.atomicabr.com.br,https://zapp-web-v3.vercel.app`; **sem localhost**; front usa `https://supabase.atomicabr.com.br` (client.ts:24, mediaUrl.ts, externalClient.ts, env.ts) | **OK** | — | exec auth (SITE_URL, URI_ALLOW_LIST); stack id=157 (Traefik rule); grep repo (src usa supabase.atomicabr.com.br; localhost só em testes/mocks) | P2: remover domínios legados da allowlist (`whats-your-line.lovable.app`, `zapp-web-v3.vercel.app`) quando forem desativados |
| 42 | Provedores externos (Google) configurados | GOTRUE_EXTERNAL_GOOGLE_ENABLED=true + client id/secret | `GOTRUE_EXTERNAL_GOOGLE_ENABLED` **vazio**; client id/secret **vazios** (fingerprint de string vazia `e3b0c44298fc`); `GOTRUE_EXTERNAL_EMAIL_ENABLED=true`, PHONE=false, ANONYMOUS=false, `GOTRUE_DISABLE_SIGNUP=true` | **DRIFT** | **P1** | exec auth: `GOOGLE_ENABLED=` (vazio), `GOOGLE_CLIENT_ID_FP=e3b0c44298fc`, `GOOGLE_SECRET_FP=e3b0c44298fc` | Configurar OAuth Google (client id/secret + enabled) se login Gmail for requisito do produto; senão documentar como intencional |
| 43 | Storage backend + credenciais + imgproxy coerentes; buckets × uso | file/S3 definido; imgproxy coerente | `STORAGE_BACKEND=file`, `FILE_STORAGE_BACKEND_PATH=/var/lib/storage` (volume); sem S3; imgproxy `IMGPROXY_URL=http://imgproxy:5001` ↔ `IMGPROXY_BIND=:5001` + `IMGPROXY_LOCAL_FILESYSTEM_ROOT=/` (coerente com file); **`IMGPROXY_KEY` vazio**; 13 buckets no DB, 5 com objetos (whatsapp-media 16.508, audio-messages 2.202, avatars 1.412, fechamentos 237, etiquetas-remessa 12); uso em code: stickers/custom-emojis/audio-memes (src), avatars/audio-messages/quarantine/whatsapp-media (edge functions) | **OK** | P2 | exec storage (BACKEND, IMGPROXY_URL, IMGPROXY_KEY_FP vazio); SQL `storage.buckets` (13) e `storage.objects` (counts); grep repo (stickers 11 refs, functions) | P2a: definir IMGPROXY_KEY/SALT (URLs de imagem hoje não assinadas); P2b: 8 buckets vazios (audio-memes, comprovantes-financeiro, custom-emojis, email-attachments, quarantine, recibos-entrega, stickers, team-chat-files) — validar se são esperados |
| 44 | Kong: rotas × serviços vivos | Toda rota aponta para serviço rodando | kong 3.9.1, `KONG_DATABASE=off` (declarativo), plugins request-transformer,cors,key-auth,acl,basic-auth,request-termination; rotas: auth-v1 (+open verify/callback/authorize), rest-v1, graphql-v1 (→rest:3000/rpc/graphql), realtime-v1-ws/rest, storage-v1, functions-v1 (+5 rotas block com request-termination: external-db-bridge, proxy-metrics, external-db-proxy, e2e-fixtures, e2e-webhook-fixture), /pg (meta, key-auth); upstreams auth:9999, rest:3000, realtime:4000, storage:5000, functions:9000 — **todos running**; /pg responde 401 com apikey anon (protegido) | **OK** | — | exec kong: rotas extraídas de /tmp/kong.yml (`KONG_YML_FP=d65d4d9106ff`); list_containers (estados running); curl functions: `PG_META=401` | JWT é validado nos serviços (PostgREST/GoTrue), não no Kong (sem plugin jwt) — padrão supabase, OK |
| 45 | Realtime: DB_HOST/schema/tenant coerentes | db:5432, usuário com replicação | `DB_HOST=db`, `DB_PORT=5432`, `DB_USER=supabase_admin` (superuser), `DB_NAME=postgres`, `DB_AFTER_CONNECT_QUERY=SET search_path TO _realtime`, `DB_ENC_KEY=supabaserealtime`, `SEED_SELF_HOST=true`, `PORT=4000`; DB: `wal_level=logical`, slots ativos `supabase_realtime_slot_realtime_` e `supabase_realtime_messages_replication_slot_` (active, wal_status=reserved); publications supabase_realtime, supabase_realtime_messages_publication; schema `_realtime` populado (tenants, extensions, feature_flags, schema_migrations) | **OK** | — | stack id=35 (realtime env); SQL `pg_settings` (wal_level=logical), `pg_replication_slots` (2 slots), `pg_publication` (3 pubs), `information_schema._realtime` | — |
| 46 | Analytics/vector/logflare configurados | logflare + vector com mesma API key | logflare 1.43.1: `LOGFLARE_SINGLE_TENANT=true`, `LOGFLARE_SUPABASE_MODE=true`, backend `postgresql://supabase_admin:<SECRETO>@db:5432/_supabase` (schema `_analytics`), API key fp `bd16beb552ef`; vector 0.53.0-alpine com docker.sock + `/etc/vector/vector.yml`, mesma API key fp `bd16beb552ef`; studio `NEXT_ANALYTICS_BACKEND_PROVIDER=logflare` + `LOGFLARE_URL=http://analytics:4000`; publicação `logflare_pub` (puballtables=true) | **OK** | — | stack id=35 (analytics/vector/studio env); SQL `pg_publication` (logflare_pub); exec (fp da API key) | — |
| 47 | Pooler/supavisor: modo e porta | transaction; porta definida | `POOLER_POOL_MODE=transaction`, `POOLER_TENANT_ID=1`, `POOLER_DEFAULT_POOL_SIZE=20`, `POOLER_MAX_CLIENT_CONN=100`, `PORT=4000`; `DATABASE_URL=ecto://postgres:***@db:5432/_supabase`; pooler.exs cria tenant `db:5432/postgres` com auth_query `pgbouncer.get_auth`; **`VAULT_ENC_KEY=your-encryption-key-32-chars-min` (placeholder)** | **OK** | P2 | stack id=35 (supavisor env); exec supavisor (pooler.exs redigido); python fp placeholder `627507f04b33` | P2: trocar `VAULT_ENC_KEY` (placeholder default) — chave de criptografia de credenciais de tenants; INFO: supavisor sem porta publicada/Traefik → só interno |
| 48 | Timezone/locale/encoding do DB | BR, UTF8 | `TimeZone=America/Sao_Paulo`, `server_encoding=UTF8`, `lc_collate=C.UTF-8`, `lc_ctype=C.UTF-8`, PostgreSQL 15.8 (imagem supabase/postgres:15.8.1.085); TZ do container vazio (timezone vem do postgresql.conf no volume `supabase_db_config`) | **OK** | — | SQL `current_setting(...)`; stack id=35 (db image/volumes) | — |
| 49 | Cross-check env↔DB: schemas do PGRST_DB_SCHEMAS existem? | 100% | Todos os 7 schemas do PGRST_DB_SCHEMAS (public, zapp, storage, graphql_public, artes, vendas, financeiro) existem no `pg_namespace` | **OK** | — | SQL `pg_namespace` (28 schemas) × stack id=35 (PGRST_DB_SCHEMAS) | — |
| 50 | Matriz CONFIG consolidada | — | Esta matriz | **OK** | — | — | — |

**Extras observados (fora da matriz, INFO):**
- `PGRST_DB_MAX_ROWS=1000` (studio) e `PGRST_DB_EXTRA_SEARCH_PATH=public,extensions` — padrão.
- `/auth/v1/health` responde 401 sem apikey (rota protegida por key-auth; health não está nas rotas open) — esperado, não é falha.
- Roles: `postgres` sem superuser (superuser é `supabase_admin`) — correto no modelo Supabase; `authenticator`/`supabase_auth_admin`/`supabase_storage_admin` com login funcional.
- `dashboard_user` (replication) existe sem login — padrão.

---

## Destaques P0 / P1

### 🟠 P1 — #42: Login Google (Gmail) NÃO configurado no GoTrue
- **Evidência:** exec no container `supabase_auth` (7252030bc705): `GOOGLE_ENABLED=` (vazio), `GOOGLE_CLIENT_ID_FP=e3b0c44298fc` (vazio), `GOOGLE_SECRET_FP=e3b0c44298fc` (vazio). Stack id=35: não há `GOTRUE_EXTERNAL_GOOGLE_*` no env do serviço `auth`.
- **Impacto:** usuários não conseguem login com conta Google (apenas email/senha, `GOTRUE_EXTERNAL_EMAIL_ENABLED=true`).
- **Ação:** se login Gmail é requisito → criar OAuth app no Google, adicionar `GOTRUE_EXTERNAL_GOOGLE_ENABLED=true`, `GOTRUE_EXTERNAL_GOOGLE_CLIENT_ID`, `GOTRUE_EXTERNAL_GOOGLE_SECRET` (via secret do Swarm) e incluir `https://supabase.atomicabr.com.br/auth/v1/callback` nos redirect URIs. Se não é requisito → documentar como decisão (status vira OK/INFO).

### ✅ P0s verificados e descartados (com evidência)
- **JWT secret divergente entre serviços:** descartado — fingerprint do secret file `b6177a99676f` == fingerprint do literal do compose `b6177a99676f` (rest/db/studio/realtime-metrics); auth/storage/functions/realtime/kong/supavisor usam o mesmo Docker secret `supabase_jwt_secret_v1`.
- **anon/service keys inválidas:** descartado — `ANON_SIG=OK` (HMAC-SHA256 com openssl in-container contra o secret file) + verificação local (anon e service_role True contra o literal) + REST `/rest/v1/` 200 e storage 200 com a anon key.
- **Schema necessário ausente do PostgREST:** descartado — os 7 schemas do PGRST_DB_SCHEMAS existem no DB.
- **Rota do Kong apontando para serviço morto:** descartado — auth/rest/realtime/storage/functions todos `running`; rotas block (request-termination) ativas.

---

## Notas de método e limitações
- `kong` NÃO é distroless nesta stack (imagem `kong:3.9.1` com sh) — rotas extraídas de `/tmp/kong.yml` via exec com redação de `key:/secret/password/token`.
- `postgrest` (rest) NÃO possui shell (`exec: "sh": executable file not found`) — env obtido via stack file + validação funcional (login authenticator OK, REST 200).
- Segredos do Swarm (`supabase_*_v1`) foram tocados apenas por fingerprint dentro dos containers; o stack file (Portainer) contém literais — citados aqui somente por fingerprint.
- O env visto via exec em containers com entrypoint `export … && exec …` não expõe as variáveis exportadas (só via stack file/command) — método documentado para reprodutibilidade.
