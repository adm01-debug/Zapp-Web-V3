name: Add zapp+evo schema stubs to types.ts

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled — schemas now generated natively
        run: |
          echo "⚠️  This workflow is disabled. Schemas zapp+evo are now included in types.ts by default."
          echo "    The add-schema-stubs workaround is no longer needed."
          exit 1
