name: Apply ChatPanel Fixes

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled — script missing
        run: |
          echo "⚠️  This workflow is disabled. The script scripts/migrate-chatpanel.mjs does not exist."
          echo "    Fix: either create the script or remove this workflow file."
          exit 1
