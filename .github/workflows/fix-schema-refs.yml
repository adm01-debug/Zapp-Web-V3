# fix-schema-refs.yml — DISABLED (migration complete)
# The public→zapp schema migration is finished.
# Only 2 intentional public schema refs remain:
#   - useAudioMessagePlayer.ts: voice_conversion_queue is physical table in public
#   - db.ts: JSDoc example comment (no runtime impact)

name: Fix schema references (DISABLED — migration complete)

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled — schema migration complete
        run: |
          echo "⚠️  Schema migration complete. Only 2 intentional public refs remain."
          echo "    Re-enable if new public schema refs are introduced."
          exit 1
