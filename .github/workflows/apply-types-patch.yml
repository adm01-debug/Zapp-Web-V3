name: Apply types.ts patch (whatsapp_connections)

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled — types are now generated via MCP
        run: |
          echo "⚠️  This workflow is disabled. types.ts is now generated via Supabase MCP or gen-types-zapp."
          echo "    The text-patching approach is brittle and replaced by full regeneration."
          exit 1
