# Declara os labels customizados dos runners self-hosted da VPS (AtomicaBR).
# Sem isto, o actionlint marca "vps-zapp"/"playwright" como labels desconhecidos
# (7 falsos positivos). Referência: github.com/rhysd/actionlint config.
self-hosted-runner:
  labels:
    - vps-zapp
    - playwright
